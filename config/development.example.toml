# Direct localhost development profile.
# Copy to zero-auth-lite.toml. Commented values show supported defaults or optional
# settings. Never use this HTTP profile in deployment.

# Root storage, process identity, and navigation settings
# runtime_dir = "/tmp/zero-auth-lite"
# snowflake_node_id = 0
# db_path = "./data/zero-auth-lite.db"
# db_echo = false
default_redirect_url = "http://localhost:8000/"

[app]
# environment = "development"
log_level = "DEBUG"
# trusted_hosts = []
# trusted_proxy_ips = []

[ui]
# authentication = "builtin"
# external_login_url = "http://localhost:3000/login"
# oauth2_interaction = "builtin"

[bootstrap]
# operator_email = "operator@example.com"
# operator_password = "Change-me-1!"
# organization_name = "Zero Auth Lite"
# first_name = "Bootstrap"
# last_name = "Operator"

[cors]
# enabled = true
allowed_origins = ["http://localhost:8000", "http://127.0.0.1:8000"]
# allow_credentials = true
# allow_methods = ["*"]
# allow_headers = ["*"]
# expose_headers = ["X-CSRF-Token", "X-Request-Id"]

[session]
# enabled = true
cookie_domain = ""
# cookie_name = "sessionid"
# cookie_same_site = "lax"
cookie_secure = false
# id_hash_secret = "dev-session-id-hash-secret-for-local-server-example"
# absolute_ttl_seconds = 604800
# cleanup_batch_size = 100
# max_sessions_per_user = 10
# slide_seconds = 1800
# ttl_seconds = 28800

[session.csrf]
cookie_domain = ""
# cookie_name = "csrftoken"
# cookie_same_site = "lax"
cookie_secure = false
# expose_token = "header"
# header_name = "X-CSRF-Token"
# origin_check_enabled = true
# pattern = "synchronizer_token"
public_origin = "http://localhost:8000"
trusted_origins = ["http://localhost:8000", "http://127.0.0.1:8000"]
# ttl_seconds = 28800

[oauth2]
# authorization_code_enabled = true
# refresh_token_enabled = true
# client_credentials_enabled = true
# device_code_enabled = true
# allow_client_secret_post = true
# jwks_enabled = true
# oidc_enabled = true
jwt_issuer = "http://localhost:8000"
# jwt_audience = "zero-auth-lite-example-api"
# jwt_key_id = "local-dev-key"
# authorization_code_ttl_seconds = 300
# access_token_lifetime_seconds = 900
# id_token_lifetime_seconds = 900
# refresh_token_lifetime_seconds = 2592000
# device_code_lifetime_seconds = 1800
# device_code_interval_seconds = 5
# device_code_create_attempts = 5
# cleanup_interval_seconds = 3600
# cleanup_batch_size = 100
# Development-only built-in secrets and raw Ed25519 key pair:
# authorization_code_hash_secret = "dev-auth-code-hash-secret-for-local-server-example"
# token_hash_secret = "dev-oauth2-token-hash-secret-for-local-server-example"
# prv_key_b64 = "8+EAUJiqpmlXFLuCF2v4LU6d8TKVp1R3JE3UwdrNhNI="
# pub_key_b64 = "37TbbJO+trhWq5Wqva6QfrOsN5DwQjy5R1evwH2vutY="
# previous_public_keys = []

[auth]
# registration_enabled = true

[auth.email]
frontend_base_url = "http://localhost:8000"

[auth.tokens]
# derivation_key_id = "default"
# derivation_secret = "dev-auth-token-derivation-secret-for-local-server-example"
# previous_derivation_secrets = [{ key_id = "previous", secret = "replace-with-a-previous-secret" }]
# verify_token_ttl_seconds = 86400
# invite_token_ttl_seconds = 604800
# reset_token_ttl_seconds = 3600

[mail]
# enabled = true
# default_from_email = "zero-auth-lite@example.com"
# default_from_name = "Zero Auth Lite"
# reply_to_email = "reply@example.com"
# template_dir = "/absolute/path/to/templates"
smtp_host = "localhost"
# smtp_port = 1025
# smtp_username = "username"
# smtp_password = "password"
# smtp_starttls = false
# smtp_ssl = false
# smtp_timeout_seconds = 10.0

[events]
# poll_interval_seconds = 1.0
# batch_size = 20
# lease_seconds = 60
# retry_max_seconds = 300
# retention_seconds = 604800
# cleanup_interval_seconds = 3600
# cleanup_batch_size = 100
# shutdown_timeout_seconds = 15.0
